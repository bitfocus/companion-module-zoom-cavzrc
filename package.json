{
  "name": "companion-module-zoom-rooms",
  "version": "0.0.3",
  "main": "dist/index.js",
  "scripts": {
    "postinstall": "husky",
    "format": "prettier -w .",
    "package": "run build && companion-module-build",
    "build": "rimraf dist && run build:main",
    "build:main": "tsc -p tsconfig.build.json",
    "dev": "tsc -p tsconfig.build.json --watch",
    "lint:raw": "eslint",
    "lint": "run lint:raw .",
    "test": "jest"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bitfocus/companion-module-zoom-cavzrc.git"
  },
  "engines": {
    "node": "^22.14",
    "yarn": "^4"
  },
  "dependencies": {
    "@companion-module/base": "1.12.1",
    "osc": "^2.4.5"
  },
  "devDependencies": {
    "@companion-module/tools": "^2.6.1",
    "@types/jest": "29",
    "@types/node": "^22.15.30",
    "eslint": "^9.39.2",
    "husky": "^9.1.7",
    "jest": "29",
    "lint-staged": "^16.2.7",
    "prettier": "^3.7.4",
    "rimraf": "^6.0.1",
    "ts-jest": "^29.4.6",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.51.0"
  },
  "prettier": "@companion-module/tools/.prettierrc.json",
  "lint-staged": {
    "*.{css,json,md,scss}": [
      "prettier --write"
    ],
    "*.{ts,tsx,js,jsx}": [
      "yarn lint:raw --fix"
    ]
  },
  "packageManager": "yarn@4.12.0"
}
